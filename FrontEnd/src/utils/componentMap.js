import ReservationForm from "../components/reservations/ReservetionForm";

const componentMap = {
  RESERVATION_FORM: ReservationForm, // Mapeo de la cadena al componente correspondiente
};

export default componentMap;
